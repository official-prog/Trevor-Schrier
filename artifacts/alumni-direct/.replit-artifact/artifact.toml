kind = "web"
previewPath = "/"
title = "AlumniDirect – Healthcare Innovation Platform"
version = "1.0.0"
id = "artifacts/alumni-direct"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20252

[services.development]
run = "pnpm --filter @workspace/alumni-direct run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/alumni-direct", "run", "build" ]
publicDir = "artifacts/alumni-direct/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20252"
BASE_PATH = "/"
